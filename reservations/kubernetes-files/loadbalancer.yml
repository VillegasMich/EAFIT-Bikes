apiVersion: v1
kind: Service
metadata:
  name: bikes-reservations-public
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  type: LoadBalancer
  selector:
    app: bikes-reservations
  ports:
    - port: 80
      targetPort: 8000